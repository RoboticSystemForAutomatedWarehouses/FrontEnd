import { MapIteratorPipe } from './map-iterator.pipe';

describe('MapIteratorPipe', () => {
  it('create an instance', () => {
    const pipe = new MapIteratorPipe();
    expect(pipe).toBeTruthy();
  });
});
